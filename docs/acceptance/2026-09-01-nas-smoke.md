# 2026-09-01 NAS Development Acceptance

环境：Mac 运行 Next.js/Node/Playwright，NAS `egocapture-dev` Compose 只运行 PostgreSQL、GoTrue、PostgREST、Storage API、API Gateway。NAS 端口 `56521/56522` 仅绑定 loopback，通过受监督 SSH Tunnel 暴露给 Mac。

## 已观察证据

- NAS 资源检查：可用内存约 9.5 GiB、`/vol1` 可用磁盘约 511 GiB。
- Compose 服务集合精确为 `db/auth/rest/storage/api-gateway`。
- Migration `0001`～`0010` 从 ledger 校验通过；`0010_demo_retention_state.sql` checksum 为 `377581dbcf27`。
- `0010` 前 schema 备份：`/vol1/1000/work/video-data-collection/backups/20260901T155247Z.sql.gz`。
- Demo Seed 连续两次执行：4 TaskVersions、4 Assignments、7 open Fixture ReviewCases、单一 current unmatched MatchDecision。
- Chromium 主流程：Participant 登录、API 越权 403、Task acknowledge、Session/Marker、真实 MP4 TUS、metadata、Admin correction、historical MatchDecision、Audit 全部通过。
- WebKit landing/upload entry smoke 通过。
- `upload:test`：约 9.8 MB MP4 跨多个 6 MiB 分片，pause/resume/findPreviousUploads、对象大小对账、metadata、损坏 MP4 分层失败通过。
- `review:test`：新旧 MatchDecision 双向 supersede、signed URL `Range: bytes=0-31` 返回 206/32 bytes、Audit 通过。
- `cron:test`：无凭据 401、过期 Intent、卡住 reconciliation、Upload Failed ReviewCase 和 Demo baseline repair 通过。
- `pnpm check`：8 个 Vitest 文件、24 个单测、ESLint、strict TypeScript、Next.js production build 通过。
- 每次受监督命令结束后，Mac `3000/56521/56522` 无监听；NAS volumes 默认保留。

## 证据边界

- 此记录不是 Vercel/Supabase Cloud 验收，不包含公开 URL 或 Supabase region。
- NAS self-hosted TUS 使用 exact-object scoped JWT workaround；不等同于 cloud official signed `x-signature` 验收。
- 真实上传最大约 9.8 MB，未验证 50 MB 边界吞吐、数 GB、4K、跨天或跨地区。
- 360 样本是合成 MP4；未使用真实 Insta360 私有 INSV 文件。
- Seed Fixture 只证明 UI/状态可复现，不证明真实异常检测能力。
